# @capacitor-community/media ships as 7.0.1 but still declares a peer dependency
# on @capacitor/core ^6.0.0, so npm install fails with ERESOLVE without this.
# The package is a Capacitor 7 release; only its peer range was left behind.
# Remove this line once that is fixed upstream.
legacy-peer-deps=true
