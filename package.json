{
  "name": "trustfirst-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Native Android/iOS shell for TrustFirst. The web app itself is served by Django; this wraps it so it can ship to app stores and reach native device features.",
  "scripts": {
    "add:android": "cap add android",
    "sync": "cap sync",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/app-launcher": "^7.0.0",
    "@capacitor/camera": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/push-notifications": "^7.0.0",
    "@capacitor/splash-screen": "^7.0.0",
    "@capacitor-community/media": "^8.0.1",
    "@capawesome/capacitor-file-picker": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
